# OS
.DS_Store
Thumbs.db
desktop.ini

# Python 字节码缓存（hook 跑过自动生成的一次性垃圾，绝不可下沉进 templates/）
__pycache__/
*.pyc

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Python 字节码缓存(templates/hooks 与 scripts 下的 .py 被执行/编译时产生)
__pycache__/
*.pyc

# Claude Code 本地配置
.claude/settings.local.json

# Hook 运行时临时产物（session_snapshot 落盘的快照 + 各 hook 日志，临时不进 git；
# 与 templates/rules/modules.md 的「.runtime/output gitignored」同理——.runtime 下
# 持久资产可 track，但快照/日志属临时产物）
.runtime/session_state/
.runtime/focus/
.runtime/*.log
