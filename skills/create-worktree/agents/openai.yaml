interface:
  display_name: "创建永久 Worktree"
  short_description: "安全创建独立分支和永久 Windows Git worktree"
  default_prompt: "使用 $create-worktree 创建一个独立分支的永久 Windows Git worktree。"

policy:
  allow_implicit_invocation: false
